diff --git a/src/dust_ray_tracing.f90 b/src/dust_ray_tracing.f90
index fc2589d4..0757993a 100644
--- a/src/dust_ray_tracing.f90
+++ b/src/dust_ray_tracing.f90
@@ -120,6 +120,7 @@ subroutine alloc_ray_tracing()
       allocate(Stokes_ray_tracing(n_lambda,1,1,RT_n_incl,RT_n_az,N_type_flux,nb_proc), stars_map(1,1,1), stat=alloc_status)
   else
      allocate(Stokes_ray_tracing(n_lambda,npix_x,npix_y,RT_n_incl,RT_n_az,N_type_flux,nb_proc), stat=alloc_status)
+     if (alloc_status > 0) call error('Allocation error Stokes_ray_tracing')
      if (lsepar_pola.and.llimb_darkening)then
         allocate(stars_map(npix_x,npix_y,3), stat=alloc_status)
      else
diff --git a/src/dust_transfer.f90 b/src/dust_transfer.f90
index 1edb82a4..9170e5ed 100644
--- a/src/dust_transfer.f90
+++ b/src/dust_transfer.f90
@@ -823,7 +823,13 @@ subroutine dust_transfer_sub()
 
         if (ind_etape==etape_f) then ! SED or spectrum writing
            call ecriture_sed(2)
-           if (lscatt_ray_tracing) call ecriture_sed_ray_tracing()
+           if (lscatt_ray_tracing) then
+              call ecriture_sed_ray_tracing()
+              if ((RT_sed_method == 2).and.(n_lambda2 == 1)) then
+                 write(*,*) "Writing coeval spatial-method-2 SED map from the same ray-tracing realization"
+                 call ecriture_map_ray_tracing()
+              endif
+           endif
            if (lProDiMo) call mcfost2ProDiMo()
            if (loutput_UV_field) call ecriture_UV_field()
            if (loutput_J) call ecriture_J(2)
diff --git a/src/init_mcfost.f90 b/src/init_mcfost.f90
index b4b65eac..b7183df1 100644
--- a/src/init_mcfost.f90
+++ b/src/init_mcfost.f90
@@ -826,6 +826,14 @@ subroutine initialisation_mcfost()
         lscatt_ray_tracing1=.false.
         lscatt_ray_tracing2=.true.
         if (.not.lmc) loutput_mc=.false.
+     case("-rt-sed-method","-rt_sed_method")
+        i_arg = i_arg + 1
+        if (i_arg > n_args) call error("1 or 2 needed for -rt-sed-method")
+        call get_command_argument(i_arg,s)
+        read(s,*,iostat=ios) RT_sed_method
+        if ((ios /= 0).or.(RT_sed_method < 1).or.(RT_sed_method > 2)) &
+             call error("-rt-sed-method must be 1 or 2")
+        i_arg = i_arg + 1
      case("-no-rt")
         i_arg = i_arg + 1
         lscatt_ray_tracing=.false.
@@ -1713,7 +1721,21 @@ subroutine initialisation_mcfost()
   else ! SED : we do not need pixels
      basename_data_dir = "data_th"
      npix_x_save = npix_x ; npix_y_save = npix_y
-     npix_x = 1 ; npix_y = 1
+     if (RT_sed_method == 1) then
+        npix_x = 1 ; npix_y = 1
+        write(*,*) "SED spatial sampler = 1 log-polar"
+     else if (RT_sed_method == 2) then
+        ! Method 2 integrates the SED over the requested Cartesian image grid.
+        ! The SED writer does not mirror the uncomputed half-map, so calculate
+        ! the complete map even when image symmetry was requested.
+        if (l_sym_ima) then
+           call warning("SED spatial method 2 requires a full Cartesian map: disabling image symmetry")
+           l_sym_ima = .false.
+        endif
+        write(*,*) "SED spatial sampler = 2 Cartesian adaptive"
+     else
+        call error("Invalid RT_sed_method")
+     endif
      lmono0 = .false.
   endif
   data_dir = trim(root_dir)//"/"//trim(seed_dir)//"/"//trim(basename_data_dir)
@@ -1786,6 +1808,11 @@ subroutine initialisation_mcfost()
      write(*,*) "Updating zoom =", zoom
   endif
 
+  if (lsed.and.(RT_sed_method == 2)) then
+     write(*,'(" SED Cartesian grid = ",I0," x ",I0,"; map = ",ES14.7," AU; zoom = ",ES14.7,"; pixel = ",ES14.7," AU")') &
+          npix_x,npix_y,map_size,zoom,(map_size/zoom)/real(max(npix_x,npix_y),kind=dp)
+  endif
+
   if (lMC_bins) then
      N_thet = ntheta
      N_phi = nazimuth
@@ -1866,6 +1893,12 @@ subroutine initialisation_mcfost()
      lscatt_ray_tracing2 = .false.
   endif
 
+  if (lscatt_ray_tracing1) then
+     write(*,*) "RT source-function method = 1"
+  else if (lscatt_ray_tracing2) then
+     write(*,*) "RT source-function method = 2"
+  endif
+
   ! Forcing azimuthal angle to 0 in rt2
   ! as angles are defined with phi=0 in mind
   if (lscatt_ray_tracing2) then
@@ -1951,6 +1984,8 @@ subroutine display_help()
   write(*,*) "        : -nphot_img : overwrite the value in the parameter file"
   write(*,*) "        : -rt : use ray-tracing method to compute images or SEDs (on by default)"
   write(*,*) "        : -rt1 or -rt2 : use ray-tracing method and force ray-tracing method"
+  write(*,*) "        : -rt-sed-method <1|2> : choose the independent SED spatial sampler"
+  write(*,*) "          method 2 with one SED wavelength also writes a coeval RT.fits.gz map"
   write(*,*) "        : -no-rt : do not output the ray-tracing results"
   write(*,*) "        : -mc :  keep Monte-Carlo output in ray-tracing mode"
   write(*,*) "        : -n_MC_bins <n_inclinations> <n_azimuth> (default : 10 1)"
diff --git a/src/output.f90 b/src/output.f90
index a24a9bde..e1cfd2a0 100644
--- a/src/output.f90
+++ b/src/output.f90
@@ -1280,6 +1280,7 @@ subroutine ecriture_sed_ray_tracing()
   logical :: simple, extend
 
   real, dimension(n_lambda2, RT_n_incl, RT_n_az, N_type_flux) :: sed_rt
+  real(kind=dp), dimension(n_lambda2, RT_n_incl, RT_n_az, N_type_flux) :: sed_rt_dp
 
   lambda=1
 
@@ -1295,8 +1296,14 @@ subroutine ecriture_sed_ray_tracing()
 
   !  Initialize parameters about the FITS image
   simple=.true.
-  ! the - sign means we write reals in the fits
-  bitpix=-32
+  ! Preserve the historical single-precision method-1 product.  The Cartesian
+  ! method-2 integral needs a double-precision accumulator and output because
+  ! it may contain millions of individually small pixel fluxes.
+  if (RT_sed_method == 1) then
+     bitpix=-32
+  else
+     bitpix=-64
+  endif
   naxis=4
   naxes(1)=n_lambda2
   naxes(2)= RT_n_incl
@@ -1309,14 +1316,16 @@ subroutine ecriture_sed_ray_tracing()
   call ftphpr(unit,simple,bitpix,naxis,naxes,0,1,extend,status)
   !call ftphps(unit,simple,bitpix,naxis,naxes,status)
 
-  sed_rt = 0.0_dp
+  sed_rt = 0.0
+  sed_rt_dp = 0.0_dp
 
   if (RT_sed_method == 1) then
      sed_rt(:,:,:,:) = sum(Stokes_ray_tracing(:,1,1,:,:,:,:),dim=5)
   else
      do j=1,npix_y
         do i=1,npix_x
-           sed_rt(:,:,:,:) = sed_rt(:,:,:,:) + sum(Stokes_ray_tracing(:,i,j,:,:,:,:),dim=5)
+           sed_rt_dp(:,:,:,:) = sed_rt_dp(:,:,:,:) + &
+                real(sum(Stokes_ray_tracing(:,i,j,:,:,:,:),dim=5),kind=dp)
         enddo
      enddo
   endif
@@ -1326,8 +1335,11 @@ subroutine ecriture_sed_ray_tracing()
   fpixel=1
   nelements=naxes(1)*naxes(2)*naxes(3)*naxes(4)
 
-  ! the e means real*4
-  call ftppre(unit,group,fpixel,nelements,sed_rt,status)
+  if (RT_sed_method == 1) then
+     call ftppre(unit,group,fpixel,nelements,sed_rt,status)
+  else
+     call ftpprd(unit,group,fpixel,nelements,sed_rt_dp,status)
+  endif
 
   call ftpkys(unit,'BUNIT',"W.m-2",'lambda.F_lambda',status)
 
